FROM python:3.11-slim

WORKDIR /app

COPY ../../governance /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    pyyaml

EXPOSE 8003

CMD ["uvicorn", "api.governance_api:app", "--host", "0.0.0.0", "--port", "8003"]